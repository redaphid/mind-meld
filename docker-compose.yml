# Mindmeld - Unified Conversation Index
#
# Quick start:
#   1. Install Ollama (https://ollama.com/download)
#   2. Run: docker compose up -d
#   Models are pulled automatically on first sync.
#
# That's it! Paths auto-detect on macOS. Linux users: see .env.example for paths.

# Pinned, not derived. Compose otherwise names the project after the directory
# you invoked it from, while every service below hardcodes its container_name —
# so reaching this repo by a second path (a symlink, a worktree, a clone under
# another name) makes `up -d` try to CREATE containers whose names are already
# taken, and it fails with a name conflict instead of recreating them.
name: mindmeld

services:
  postgres:
    image: ghcr.io/redaphid/mindmeld-postgres:latest
    container_name: mindmeld-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: mindmeld
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mindmeld}
      POSTGRES_DB: conversations
    ports:
      - "${POSTGRES_PORT:-5433}:5432"
    volumes:
      - mindmeld-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mindmeld -d conversations"]
      interval: 10s
      timeout: 5s
      retries: 5

  chroma:
    image: chromadb/chroma:latest
    container_name: mindmeld-chroma
    restart: unless-stopped
    environment:
      CHROMA_SERVER_AUTHN_PROVIDER: ""
      IS_PERSISTENT: "TRUE"
      ANONYMIZED_TELEMETRY: "FALSE"
      CHROMA_SERVER_CORS_ALLOW_ORIGINS: '["*"]'
    ports:
      - "${CHROMA_PORT:-8001}:8000"
    volumes:
      - mindmeld-chroma:/data
    healthcheck:
      test: ["CMD", "bash", "-c", "exec 3<>/dev/tcp/localhost/8000"]
      interval: 10s
      timeout: 5s
      retries: 5

  # NOTE: the `sync` worker no longer runs in Docker on macOS. Docker Desktop's
  # container network proxy (gvisor/vpnkit) backpressures Ollama's response
  # stream, inflating chunk summaries from ~3s to ~30s. It now runs as a HOST
  # process via scripts/sync-host.sh (launchd: com.example.mindmeld-sync),
  # reaching Ollama over the fast host->tunnel path and Postgres/Chroma via their
  # published ports. On a Linux host where Ollama is local, restore this service.

  centroid-compute:
    image: ghcr.io/redaphid/mindmeld-centroids:latest
    container_name: mindmeld-centroids
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      chroma:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mindmeld}
      CHROMA_HOST: chroma
      CHROMA_PORT: 8000
      OLLAMA_URL: ${OLLAMA_URL:-http://host.docker.internal:11434}
      OLLAMA_MAX_CONCURRENCY: ${OLLAMA_MAX_CONCURRENCY:-1}
      # Centroids are averages of existing vectors, so the embedding model must
      # match the one that produced them; vectors from two models are not
      # comparable.
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-bge-m3}
      CENTROID_INTERVAL_SECONDS: ${CENTROID_INTERVAL_SECONDS:-25200}

  warmup-filter:
    image: ghcr.io/redaphid/mindmeld-warmups:latest
    container_name: mindmeld-warmups
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      chroma:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mindmeld}
      CHROMA_HOST: chroma
      CHROMA_PORT: 8000
      WARMUP_INTERVAL_SECONDS: ${WARMUP_INTERVAL_SECONDS:-21600}

  mcp:
    image: ghcr.io/redaphid/mindmeld-mcp:latest
    container_name: mindmeld-mcp
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      chroma:
        condition: service_healthy
    environment:
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mindmeld}
      CHROMA_HOST: chroma
      CHROMA_PORT: 8000
      OLLAMA_URL: ${OLLAMA_URL:-http://host.docker.internal:11434}
      OLLAMA_MAX_CONCURRENCY: ${OLLAMA_MAX_CONCURRENCY:-1}
      # This service drains the embedding queue too, so it needs the same model
      # config as any sync worker. Without these it silently used the defaults in
      # src/config.ts while a host sync read .env — two workers on one queue
      # summarizing the same sessions with different models.
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-bge-m3}
      SUMMARIZE_MODEL: ${SUMMARIZE_MODEL:-qwen3:4b-instruct}
      MCP_PORT: 3000
      # Reported as `thisMachine` by /logs; without it this falls back to the
      # container id, which tells nobody anything.
      MACHINE_NAME: ${MACHINE_NAME:-windows}
    ports:
      - "${MCP_HTTP_PORT:-3847}:3000"

  # The browser UI as its own container. Serves public/ at the root and
  # reverse-proxies the API surface (/mcp, /api, /status, /health, /logs,
  # /openapi.yaml) to the mcp service — so the Cloudflare tunnel's public
  # hostname (e.g. mindmeld.example.com) points at http://ui:3000 with a single
  # ingress rule and every path keeps working. The mcp service remains fully
  # standalone on ${MCP_HTTP_PORT:-3847} for MCP clients.
  ui:
    image: ghcr.io/redaphid/mindmeld-ui:latest
    container_name: mindmeld-ui
    restart: unless-stopped
    depends_on:
      - mcp
    environment:
      UI_PORT: 3000
      UPSTREAM_URL: http://mcp:3000
      # Same contract as the mcp service: comma-separated hostnames added to
      # the localhost defaults. The tunnel hostname must be listed here.
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-}
    ports:
      - "${UI_HTTP_PORT:-3848}:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3

  # Optional: Cloudflare tunnel for remote access
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: mindmeld-cloudflared
    restart: unless-stopped
    depends_on:
      - mcp
      - ui
    command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
    profiles:
      - tunnel

volumes:
  mindmeld-postgres:
  mindmeld-chroma:

networks:
  default:
    name: mindmeld-network

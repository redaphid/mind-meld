# Noise steering status board

`board.svg` is a snapshot of the live board kept while the noise-steering PRs
were built: the plan, measured findings, and the similarity charts the eval
(`pnpm run noise:eval`) produces. `board.excalidraw` is its source; open it at
excalidraw.com to edit. Snapshots are committed at each milestone.

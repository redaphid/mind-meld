# Dependencies
node_modules/

# Build output
dist/
out/

# Environment
.env
.env.local

# Local Docker overrides
docker-compose.override.yml

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
logs/

# Stray Claude version symlinks
/2.1.*


# Local pnpm store (appears in-project when the global store is on another drive)
.pnpm-store/

# Temporary files
tmp/
*.tmp

# Dumped conversation content. summarize-transcript.ts reads a transcript and
# writes a summary; both are verbatim conversation text and this repo is public.
# tmp/ already covers the documented paths — these catch the stray dump in the
# repo root before `git add .` does.
transcript*.json
transcript*.jsonl
summary*.txt

# Test coverage
coverage/

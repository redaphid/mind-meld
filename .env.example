# Mindmeld Configuration
# Most users don't need this file - defaults work on macOS.
# Linux users: uncomment CURSOR_GLOBALSTATE_PATH below.

# ============================================
# LINUX USERS ONLY
# ============================================
# Uncomment and set your Cursor path:
# CURSOR_GLOBALSTATE_PATH=~/.config/Cursor/User/globalStorage

# ============================================
# OPTIONAL: Override defaults
# ============================================

# Database password (default: mindmeld)
# POSTGRES_PASSWORD=your-secure-password

# Ports (defaults avoid conflicts with common services)
# POSTGRES_PORT=5433
# CHROMA_PORT=8001
# MCP_HTTP_PORT=3847

# Embedding model (default: bge-m3)
# EMBEDDING_MODEL=bge-m3
# EMBEDDING_DIMENSIONS=1024

# Summarization model (default: qwen3:4b)
# SUMMARIZE_MODEL=qwen3:4b

# Sync interval in seconds (default: 3600 = 1 hour)
# SYNC_INTERVAL_SECONDS=3600

# ============================================
# ADVANCED: Custom Ollama URL
# ============================================
# Default uses host Ollama via host.docker.internal
# Change if Ollama is on a different machine:
# OLLAMA_URL=http://192.168.1.100:11434

# ============================================
# CLOUDFLARE TUNNEL (optional)
# ============================================
# For remote access:
# docker compose --profile tunnel up -d
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token

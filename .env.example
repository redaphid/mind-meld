# Mindmeld Configuration
# Copy this to .env and customize as needed
# Most settings have sensible defaults - only paths are required

# ============================================
# REQUIRED: Source paths for your conversations
# ============================================

# Claude Code conversations (usually ~/.claude)
CLAUDE_CODE_PATH=~/.claude

# Cursor conversations (platform-specific)
# macOS: ~/Library/Application Support/Cursor/User/globalStorage
# Linux: ~/.config/Cursor/User/globalStorage
# Windows: %APPDATA%/Cursor/User/globalStorage
CURSOR_GLOBALSTATE_PATH=~/Library/Application Support/Cursor/User/globalStorage

# ============================================
# OPTIONAL: Override defaults if needed
# ============================================

# Database password (default: mindmeld)
# POSTGRES_PASSWORD=your-secure-password

# Ports (defaults avoid conflicts with common services)
# POSTGRES_PORT=5433
# CHROMA_PORT=8001
# OLLAMA_PORT=11434
# MCP_HTTP_PORT=3847

# Embedding model (default: bge-m3, good balance of speed/quality)
# EMBEDDING_MODEL=bge-m3
# EMBEDDING_DIMENSIONS=1024

# Summarization model (default: qwen3:4b, fast and capable)
# SUMMARIZE_MODEL=qwen3:4b

# Sync interval in seconds (default: 3600 = 1 hour)
# SYNC_INTERVAL_SECONDS=3600

# Centroid computation interval (default: 25200 = 7 hours)
# CENTROID_INTERVAL_SECONDS=25200

# ============================================
# EXTERNAL OLLAMA (optional)
# ============================================
# If you already have Ollama running, point to it:
# OLLAMA_URL=http://host.docker.internal:11434
#
# Then disable the built-in Ollama by adding to docker compose:
# docker compose up -d --scale ollama=0

# ============================================
# CLOUDFLARE TUNNEL (optional)
# ============================================
# For remote access, add your tunnel token and use:
# docker compose --profile tunnel up -d
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token

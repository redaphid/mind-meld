{
  "name": "mcp-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OAuth 2.1 gateway that fronts self-hosted MCP servers, so the servers themselves need no auth code",
  "scripts": {
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.120.0"
  }
}
